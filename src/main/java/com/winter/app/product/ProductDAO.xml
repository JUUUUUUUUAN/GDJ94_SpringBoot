<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="com.winter.app.product.ProductDAO">
    	<!-- 상세 정보 조회 -->
    	<insert id="add" parameterType="ProductDTO">
    		INSERT INTO products (product_name, product_contents, product_category, product_rate, product_sale)
    		VALUES (#{productName}, #{productContents}, #{productCategory}, #{productRate}, #{productSale})
    	</insert>
    	<select id="list" resultType="ProductDTO">
    		SELECT * FROM products
    	</select>
    	<select id="detail" parameterType="ProductDTO" resultType="ProductDTO">
    		SELECT * FROM products WHERE product_num=#{productNum}
    	</select>
    	<update id="update" parameterType="ProductDTO">
    		UPDATE products 
    		SET product_name=#{productName}, product_contents=#{productContents}, product_category=#{productCategory}, product_rate=#{productRate}, product_sale=#{productSale}
    		WHERE product_num=#{productNum}  
    	</update>
    </mapper>