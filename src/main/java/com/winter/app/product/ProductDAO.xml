<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="com.winter.app.product.ProductDAO">
    	<!-- 상세 정보 조회 -->
    	<insert id="add" parameterType="ProductDTO">
    		INSERT INTO product (product_name, product_contents, product_category, product_rate, product_sale)
    		VALUES (#{productName}, #{productContents}, #{productCategory}, #{productRate}, #{productSale})
    	</insert>
    	<select id="list" resultType="ProductDTO">
    		SELECT * FROM product
    	</select>
    </mapper>