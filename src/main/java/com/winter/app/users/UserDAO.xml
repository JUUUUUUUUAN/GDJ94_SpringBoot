<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="com.winter.app.users.UserDAO">
		<insert id="register" parameterType="UserDTO">
		<!-- social login의 경우 enabled는 0으로 처리되게끔 -->
			INSERT INTO users(username, password, name, email, phone, birth ) VALUES (#{username}, #{password}, #{name}, #{email}, #{phone}, #{birth});
		</insert>
		<!-- fileAdd -->
		<insert id="fileAdd" parameterType="UserFileDTO">
			<!-- auto increment 처리 = null 기입해줘야 함 -->
			INSERT INTO profile VALUES (null, #{fileName}, #{fileOrigin}, #{username})
		</insert>
		<insert id="roleAdd" parameterType="UserDTO">
			INSERT INTO user_role (username, role_num) VALUES(#{username}, 3)
		</insert>
		
		<select id="detail" resultMap="detailResult" resultType="UserDTO">
    		SELECT * FROM users u 
    		LEFT JOIN profile p ON u.username = p.username 
    		LEFT JOIN user_role ur ON u.username=ur.username 
    		LEFT JOIN role r USING(role_num) WHERE u.username=#{username}
    	</select>
    	<resultMap type="UserDTO" id="detailResult">
    		<id column="username" property="username"/>
    		<result column="password" property="password"/>
    		<result column="name" property="name"/>
    		<result column="email" property="email"/>
    		<result column="phone" property="phone"/>
    		<result column="birth" property="birth"/>
    		<result column="account_non_expired" property="accountNonExpired"/>
    		<result column="account_non_locked" property="accountNonLocked"/>
    		<result column="credentials_non_expired" property="credentialsNonExpired"/>
    		<result column="enabled" property="enabled"/>
    		<association property="userFileDTO" javaType="UserFileDTO">
    			<id column="file_num" property="fileNum"/>
    			<result column="file_name" property="fileName"/>
    			<result column="file_origin" property="fileOrigin"/>
    		</association>
    		<collection property="roleDTOs" javaType="java.util.List" ofType="RoleDTO">
    			<id column="role_num" property="roleNum"/>
    			<result column="role_name" property="roleName"/>
    		</collection>
    	</resultMap>
    	
    	<update id="update" parameterType="UserDTO">
    		UPDATE users
    		<set>
    			name=#{name}, 
    			<if test="email != null">
    				email=#{email},    			
    			</if>
    			<if test="phone != null">
    				phone=#{phone},    			
    			</if>
    			<if test="birth != null">
    				birth=#{birth}    			
    			</if>
    		</set>
    		WHERE username=#{username}
    	</update>
    	
	</mapper>