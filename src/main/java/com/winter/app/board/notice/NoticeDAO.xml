<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="com.winter.app.board.notice.NoticeDAO">
    	<!-- 반복되는 쿼리를 모아논 것 -->
    	<sql id="search">
    	    <if test="kind == 'k1'">
    			WHERE board_title LIKE concat('%',#{search},'%')
    		</if>
    		<if test="kind == 'k2'">
    			WHERE board_contents LIKE concat('%',#{search},'%')    		
    		</if>
    		<if test="kind == 'k3'">
    			WHERE board_writer LIKE concat('%',#{search},'%')    		
    		</if>
    	</sql>
    	<select id="count" resultType="Long" parameterType="Pager">
    		SELECT count(board_num) FROM notice
			<include refid="search"></include>
    	</select>
    	<select id="list" resultType="NoticeDTO" parameterType="Pager">
    		SELECT * 
    		FROM notice
			<include refid="search"></include>
    		ORDER BY board_num DESC
    		LIMIT #{startNum}, #{perPage}
    	</select>
    	<update id="update" parameterType="NoticeDTO">
    		UPDATE notice SET board_title=#{boardTitle}, board_writer=#{boardWriter}, board_contents=#{boardContents} WHERE board_num=#{boardNum};
    	</update>
    	<delete id="fileDelete" parameterType="BoardDTO">
    		DELETE FROM notice_files WHERE board_num=#{boardNum}
    	</delete>
    	<delete id="delete" parameterType="NoticeDTO">
    		DELETE FROM notice WHERE board_num=#{boardNum} 
    	</delete>
    	<insert id="add" parameterType="NoticeDTO"  useGeneratedKeys="true" keyProperty="boardNum">
	    	INSERT INTO notice(board_title, board_writer, board_date, board_contents) 
	    	VALUES(#{boardTitle},#{boardWriter},now(),#{boardContents})
    	</insert>
    	
    	<select id="detail" parameterType="NoticeDTO" resultMap="detailResult">
    		SELECT *
			FROM notice n
			LEFT JOIN notice_files nf
			USING(board_num)
			WHERE n.board_num=#{boardNum};
    	</select>
    	<resultMap type="NoticeDTO" id="detailResult">
    		<id column="board_num" property="boardNum"/>
    		<result column="board_title" property="boardTitle"/>
    		<result column="board_writer" property="boardWriter"/>
    		<result column="board_contants" property="boardContents"/>
    		<result column="board_date" property="boardDate"/>
    		<result column="board_hit" property="boardHit"/>
    		<collection property="fileDTOs" javaType="java.util.List" ofType="BoardFileDTO">
    			<id column="file_num" property="fileNum"/>
    			<result column="file_name" property="fileName"/>
    			<result column="file_origin" property="fileOrigin"/>
    		</collection>
    	</resultMap>
    	
    	<!-- fileAdd -->
    	<insert id="fileAdd" parameterType="BoardFileDTO">
    		INSERT INTO notice_files VALUES (null, #{fileName}, #{fileOrigin}, #{boardNum})
    	</insert>
    	
    	<select id="fileDetail" parameterType="BoardFileDTO" resultType="BoardFileDTO">
    		SELECT * FROM notice_files WHERE file_num=#{fileNum}
    	</select>
    </mapper>